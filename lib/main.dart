import 'package:flutter/material.dart';

import 'myapp.dart';

void main() {
  // ToDo: Add Widgets Binding
  // ToDo: Init Local Storage
  // ToDo: Init Firebase
  // ToDo: Init Auth
  runApp(MyApp());
}
